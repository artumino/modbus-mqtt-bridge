[package]
name = "modbus-mqtt-bridge-core"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
defmt = { version = "1.0", optional = true }
rust-mqtt = { version = "0.3.0", default-features = false }
rmodbus = { version = "0.10.2", features = ["heapless"], default-features = false }
rand_core = "0.6.4"
error_set = { version = "0.8.5", default-features = false }
serde = { version = "1.0.219", default-features = false, features = ["derive"] }
heapless = "0.8.0"
embedded-io-async = { version = "0.6.1", optional = true }

embassy-time = { version = "0.4.0", default-features = false, optional = true }
log = { version = "0.4.27", optional = true }
tokio = { version = "1", features = ["time", "io-util"], optional = true }

[features]
default = ["std", "tokio", "embedded-io-async"]
embedded-io-async = ["dep:embedded-io-async"]
embassy = ["embedded-io-async", "rust-mqtt/no_std", "defmt"]
defmt = ["dep:defmt", "embedded-io-async?/defmt-03", "rust-mqtt/defmt", "embassy-time/defmt", "embassy-time/defmt-timestamp-uptime", "error_set/defmt", "rmodbus/defmt"]
log = ["dep:log", "rust-mqtt/log", "embassy-time/log", "error_set/log"]
std = ["log", "embassy-time/std",  "rust-mqtt/default"]
tokio = ["dep:tokio"]
